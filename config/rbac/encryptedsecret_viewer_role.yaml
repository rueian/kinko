# permissions for end users to view encryptedsecrets.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: encryptedsecret-viewer-role
rules:
- apiGroups:
  - secrets.kms-secrets-operator
  resources:
  - encryptedsecrets
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - secrets.kms-secrets-operator
  resources:
  - encryptedsecrets/status
  verbs:
  - get
